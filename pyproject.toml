[project]
name = "multi-agent-orchestrator"
version = "0.1.0"
description = "Provider-agnostic LLM orchestration with automatic failover, cost tracking and spend caps, plus a natural-language-to-SQL agent and a code review agent built on top."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.116.0",
    "faker>=40.36.0",
    "fastapi>=0.115.0",
    "google-genai>=1.0.0",
    "openai>=2.44.0",
    "psycopg2-binary>=2.9.12",
    "python-dateutil>=2.9.0.post0",
    "python-dotenv>=1.2.2",
    "uvicorn[standard]>=0.34.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/orchestrator"]

[tool.pytest.ini_options]
pythonpath = ["src", "tests"]
testpaths = ["tests"]
